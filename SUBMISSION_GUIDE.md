# Complete Submission & Deployment Guide

## 1. MongoDB Atlas (Already Setup)
- Cluster: cluster0 (your URI).
- DB: military_assets (auto-created).
- Users/Assets seeded.
- Atlas dashboard: https://cloud.mongodb.com → Collections → View data.

## 2. Git Push to Repo
```
git init
git add .
git commit -m "Complete project with all features"
git branch -M main
git remote add origin https://github.com
